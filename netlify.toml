[build]
  command = "npm install && npm run generate"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect - all routes should serve index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

